- NEVER run migrations unless explicitly asked.
- NEVER make git changes (such as commits or pushes) unless explicitly asked.
  (Read-only operations such as `git status` are OK.)
- NEVER make file system changes outside of the current working directory/directories unless explicitly asked.
- NEVER attempt to debug or fix errors when running commands (i.e. missing CLI tools or dependencies)
  unless explicitly asked. Simply report the error and stop.
