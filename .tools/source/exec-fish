#!/bin/false
# shellcheck shell=sh

# replace running shell with fish, unless FISH_DISABLE is set.
# this file must be sourced, not executed, to work properly.

if [ -z "$FISH_DISABLE" ]; then
  if rc__command_exists fish; then
    exec fish
  fi
fi
